const c = bg, x = c.getContext('2d'); function r() { c.width = innerWidth; c.height = innerHeight } r(); onresize = r;
      let lines = [...Array(25)].map(() => ({ x: Math.random() * c.width, y: Math.random() * c.height, l: 100 + Math.random() * 150, a: Math.random() * 6.28, s: .2 + Math.random() * .5, o: .05 + Math.random() * .1 }));
      gsap.ticker.add(() => { x.clearRect(0, 0, c.width, c.height); lines.forEach(n => { n.x += Math.cos(n.a) * n.s; n.y += Math.sin(n.a) * n.s; if (n.x < 0 || n.x > c.width) n.a = Math.PI - n.a; if (n.y < 0 || n.y > c.height) n.a = -n.a; x.beginPath(); x.moveTo(n.x, n.y); x.lineTo(n.x + Math.cos(n.a) * n.l, n.y + Math.sin(n.a) * n.l); x.strokeStyle = 'rgba(37,99,235,' + n.o + ')'; x.stroke(); }); });
      document.querySelectorAll('.menu a').forEach(a => a.onclick = e => { e.preventDefault(); if (a.classList.contains('active')) return; document.querySelector('.menu a.active').classList.remove('active'); a.classList.add('active'); let cur = document.querySelector('.pane.active'), nxt = document.getElementById(a.dataset.tab); gsap.to(cur, { opacity: 0, y: -20, duration: .25, onComplete() { cur.classList.remove('active'); nxt.classList.add('active'); gsap.fromTo(nxt, { opacity: 0, y: 20 }, { opacity: 1, y: 0, duration: .45 }); } }); });
